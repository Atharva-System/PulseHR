# Session: 2026-04-15
- Investigated empty styling on Dashboard and tickets tables.
- Found out that Tailwind CSS v4 uses standard CSS `@layer` rules.
- Wrapped user's global manual resets in `@layer base` inside `index.css` to prevent `* { padding: 0 }` from winning over Tailwind's layered utility classes like `px-6`. CSS specificity is restored.
